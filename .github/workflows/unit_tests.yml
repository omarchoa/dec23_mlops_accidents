name: Unit Tests

on:
  push:
    branches:
      - ci/unit-testing
  pull_request:
    branches:
      - ci/unit-testing

jobs:
  test:
    runs-on: ubuntu-latest  # Vous pouvez choisir un autre système d'exploitation si nécessaire

    services:
      docker:
        image: docker:latest
        options: --privileged

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build Docker images
      run: docker-compose -f docker-compose-dev.yml build

    # - name: Run unit tests for dummy microservice
    #   run: docker-compose -f docker-compose-dev.yml run --rm dummy pytest tests/  # Assurez-vous que le chemin vers vos tests est correct

    # - name: Run unit tests for data microservice
    #   run: docker-compose -f docker-compose-dev.yml run --rm data pytest tests/

    # - name: Run unit tests for training microservice
    #   run: docker-compose -f docker-compose-dev.yml run --rm training pytest tests/

    # - name: Run unit tests for prediction microservice
    #   run: docker-compose -f docker-compose-dev.yml run --rm prediction pytest tests/

    # - name: Run unit tests for scoring microservice
    #   run: docker-compose -f docker-compose-dev.yml run --rm scoring pytest tests/
