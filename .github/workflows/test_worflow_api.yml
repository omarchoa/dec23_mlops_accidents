name: Unit Tests

on:
  push:
    branches:
      - master
      - docker
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r test_requirements.txt
    - name: Change working directory
      run: cd src/features/api
    - name: Run unit tests
      run: python -m unittest src/features/api/test_api.py
